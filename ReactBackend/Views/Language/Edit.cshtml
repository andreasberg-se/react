@model EditLanguageViewModel

@{
    ViewData["Title"] = "Edit Language";
}

<h2>@ViewBag.Title</h2>

@if (ViewBag.Message != null)
{
    <p><strong>@ViewBag.Message</strong></p>
}

@if ((User.IsInRole("Admin")) || (User.IsInRole("Moderator")))
{
    if (@Model == null)
    {
        <p><strong>Language not found!</strong></p>
    }
    else
    {
        <form asp-controller="Language" asp-action="Edit" method="post">

            <div asp-validation-summary="ModelOnly"></div>

            <label asp-for="LanguageId" hidden="true"></label>
            <input asp-for="LanguageId" hidden="true" />

            <label asp-for="LanguageName" class="left"></label>
            <input asp-for="LanguageName" />
            <label class="left"></label>
            <span asp-validation-for="LanguageName" class="validation"></span>

            <label class="left">&nbsp;</label>
            <input type="submit" value="Edit language" class="create" />

        </form>
    }
}

<p><a asp-controller="Person" asp-action="Index">Back to index</a></p>
